[zentao-api](../index.md) / ModuleActionResultFieldMap

# Type Alias: ModuleActionResultFieldMap

> **ModuleActionResultFieldMap** = `Readonly`\<`Record`\<`string`, `string`\>\>

从原始响应中重映射业务数据字段的映射表。
键为输出字段名，值为原始响应中的字段路径（支持 `a.b` 嵌套）。
